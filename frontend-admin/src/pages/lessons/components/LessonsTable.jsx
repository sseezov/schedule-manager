export default function LessonsTable({ data }) {
  const { groups, lessons, teachers, subjects } = data;
  console.log(data);
  return (
    <table>
      <thead>

      </thead>
      <tbody>

      </tbody>
    </table>

  )
}
